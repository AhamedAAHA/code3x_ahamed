# code3x login task

my internship task login page.
made with react + vite + typescript + mui.

how to run:
```
npm install
npm run dev
```

firebase:
- made project in firebase console
- enabled google login in authentication
- copy config to src/firebase.ts

deploy:
```
npm run build
firebase deploy
```
